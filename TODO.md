# Travel Together 任务清单

> ## 当前暂停点（恢复开发时先处理）
> - 自动任务推进已暂停；不会再在凌晨自动改代码或部署。
> - 线上 `GET /api/health` 此前返回 **502 Bad Gateway**，根因为 systemd 直接 `python3 app.py` 启动而 Flask-SocketIO 缺少 `allow_unsafe_werkzeug=True`，被 Werkzeug >= 2.3 生产模式保护拒绝。
> - ✅ 已修复：`socketio.run(...)` 已加 `allow_unsafe_werkzeug=True`，并新增 `tests/test_startup_regression.py` 启动回归测试。
> - 恢复后下一步：推送后等待 GitHub Actions 成功 → 验证 `/api/health` 返回 JSON → 继续在 1Panel/Nginx 配置 `location /socket.io/` 反代到后端 5000，用双浏览器验收实时协作。

## 第一阶段：基础架构 ✅

- [x] 修复 app.js 缺失变量定义
- [x] 前端改用后端 API 读写数据
- [x] 动态生成天数（根据项目日期）
- [x] index.html 加认证检查
- [x] 后端加文件锁防并发
- [x] 修复 substr → substring

## 第二阶段：AI 机器人接口 ✅

- [x] AI 批量导入旅行块 API
- [x] AI 修改行程 API
- [x] AI 创建投票 API
- [x] AI 读取项目 API
- [x] AI 认证机制（API Key）
- [x] AI 接口文档

## 第三阶段：成员管理 ✅

- [x] 用户注册/登录系统
- [x] 邀请成员 API
- [x] 角色权限（Owner/Editor/Viewer）
- [x] 成员列表 API
- [x] 移除成员 API
- [x] **前端成员管理界面** - 在项目卡片上添加"管理成员"按钮（4d09f1e）
- [x] **成员管理弹窗** - 显示成员列表、邀请表单、角色修改（4d09f1e）
- [x] **项目卡片操作按钮** - 编辑、删除、管理成员（4d09f1e）

## 第四阶段：核心功能优化 🔴 高优先级

### 行程编辑优化
- [x] **拖拽排序** - 同一天内调整行程顺序（785d41c）
- [x] **跨天拖拽** - 从一天拖到另一天（785d41c）
- [x] **撤销/重做** - Ctrl+Z / Ctrl+Y 支持（本次）
- [x] **自动保存** - 每次编辑后自动保存到后端（本次）
- [x] **编辑冲突提示** - 乐观锁检测并让用户选择加载最新或覆盖保存（本次）
- [x] **时间冲突检测** - 同一天同一开始时间的活动显示无障碍警告（本次）

### 投票决策优化
- [x] **多选投票** - 支持自定义多个选项、逐项统计与单人改投（本次）
- [x] **投票截止时间** - 创建时可设置截止时间，截止后禁投并保留历史结果（本次）
- [x] **投票结果通知** - 投票结束后通知所有成员（1cc675d）
- [x] **投票统计图表** - 可视化显示投票结果（3a218f1）
- [x] **投票评论** - 投票时可以添加评论说明原因（dc66933）

### 评论系统 🔴 核心缺失
- [x] **行程项评论** - 每个行程项可以评论
- [x] **@提及成员** - 评论中可以 @其他成员
- [x] **评论通知** - 被 @或回复时收到通知（9114c5f）
- [x] **评论点赞** - 对评论点赞
- [x] **评论编辑/删除** - 编辑或删除自己的评论

### 实时协作 🔴 需完成反向代理后验收
- [ ] **WebSocket 实时同步** - 前后端基础设施已实现；需 Nginx 代理 `/socket.io/` 并进行双浏览器验收
- [ ] **在线成员显示** - UI 与房间成员状态已实现；依赖 WebSocket 生产连通性
- [ ] **光标位置同步** - 事件与显示逻辑已实现；依赖 WebSocket 生产连通性
- [ ] **操作广播** - 事件与提示逻辑已实现；依赖 WebSocket 生产连通性
- [ ] **断线重连** - Socket.IO 重连配置已实现；依赖 WebSocket 生产连通性

## 第五阶段：新功能 🟡 中优先级

### 通知系统
- [x] **通知中心** - 页面右上角显示未读通知（已接入后端接口）
- [ ] **邮件通知** - 重要事件发送邮件通知
- [ ] **浏览器推送** - 使用 Web Push API（需 HTTPS 与 VAPID 密钥）
- [x] **通知设置** - 可设置 @提及、投票、系统提醒开关

### 导出分享
- [ ] **导出为 PDF** - 生成精美的行程 PDF
- [ ] **导出为图片** - 生成行程长图
- [ ] **分享链接** - 生成只读分享链接（无需登录）
- [ ] **嵌入代码** - 生成 iframe 嵌入代码

### 地图集成
- [x] **行程地图** - 在地图上显示所有行程点（高德 JS API）（ec62d84）
- [x] **地图选点** - 编辑行程项时可在地图上点击选择坐标（ec62d84）
- [x] **地图标记** - 在地图上标记有坐标的行程点（自定义编号标记）（ec62d84）
- [ ] **路线规划** - 显示景点之间的路线
- [ ] **距离计算** - 计算景点之间的距离和时间

### 预算管理
- [ ] **预算设置** - 设置总预算和每日预算
- [ ] **费用记录** - 记录每项活动的费用
- [ ] **费用分摊** - 多人费用分摊计算
- [ ] **预算统计** - 显示预算使用情况

### 文件上传
- [ ] **自定义旅行块图片** - 上传自定义图片
- [ ] **行程附件** - 上传机票、酒店预订单等
- [ ] **相册功能** - 上传旅行照片

## 第六阶段：体验优化 🟢 进行中

### ️ 画布交互增强
- [ ] **1. 点击空白处快速新增**
  - 点击天的空白区域（drop-zone 内无卡片的位置），在点击处弹出小气泡
  - 气泡内容：①「新建行程」→ 打开 add-slot 对话框，自动填入当前天数和估算时间 ②「从灵感库选」→ 展开灵感库小列表供快速选择
  - 点击气泡外部或按 Esc 关闭气泡
  - 气泡出现位置：紧邻点击处，不超出画布边界
  - 动画：气泡从点击处 scale(0.5) → scale(1) + opacity 0→1，120ms ease-out
- [ ] **2. 双击旅行块直接编辑**
  - 双击卡片上的名称区域，进入内联编辑模式
  - 名称变为 input 框，自动全选文字，回车/失焦保存，Esc 取消
  - 双击时间区域，直接弹出时间选择器（不打开详情弹窗）
  - 编辑时卡片加轻微高亮边框
- [ ] **3. 拖拽删除**
  - 从卡片拖出到画布底部/左边缘时，出现半透明红色删除区域
  - 拖入删除区域后松开 → 删除该行程项（不删除旅行块）
  - 拖入时删除区域高亮 + 图标放大反馈
  - 删除前有短暂动画过渡（卡片缩小淡出）
- [ ] **4. 拖拽调整时间**
  - 上下拖动卡片改变时间：向上 = 时间提前，向下 = 时间推后
  - 拖动时显示当前时间浮标（跟随鼠标）
  - 每拖动 20px = 时间变化 15 分钟
  - 松开后自动吸附到最近的 5 分钟刻度
  - 拖动过程中实时保存到后端

### ✨ 弹窗动画
- [ ] **5. 所有 dialog 加缓动效果**
  - backdrop：opacity 0→1，150ms ease-out
  - dialog 本体：从底部 translateY(20px) → translateY(0) + scale(0.96) → scale(1)，180ms cubic-bezier(0.34, 1.56, 0.64, 1)（微弹效果）
  - 关闭时反向动画：opacity 1→0 + translateY(0) → translateY(10px)，120ms ease-in
  - 关闭按钮（×）：hover 时旋转 90°，150ms ease
  - 适用范围：所有 `<dialog>` 元素（add-block、location-picker、branch、poll、travel-detail 等）

### ⌨️ 效率提升
- [ ] **6. 键盘快捷键**
  - `Delete` / `Backspace`：删除当前选中的行程卡片
  - `N`：新建行程（打开 add-slot 对话框）
  - `Z`（Ctrl/Cmd+Z）：撤销上一步操作
  - `1` ~ `5`：快速切换/滚动到对应天数
  - `Escape`：关闭当前打开的弹窗/气泡
  - `M`：切换地图/时间线视图
  - 快捷键提示：首次访问显示一个小浮层提示可用快捷键，之后不再显示
- [ ] **7. 选中态与键盘导航**
  - 单击卡片 → 选中（加绿色边框高亮）
  - 选中后可用 `↑` `↓` 切换卡片
  - 选中后按 `Enter` 打开详情弹窗
  - 画布空白处点击取消选中
- [ ] **8. 新建后自动聚焦**
  - 新建行程卡片后，自动聚焦到名称输入框
  - 光标置于末尾，用户可直接打字

### 👁️ 视觉反馈
- [ ] **9. 空天提示优化**
  - 空白天的 drop-hint 改为「点击添加行程」，带一个 ＋ 图标
  - hint 样式改为更醒目的虚线圆角框，而非纯文字
- [ ] **10. 路线预览实时数据**
  - 拖拽连接两个旅行块时，连线中点显示预估距离和时间（调用高德驾车/步行 API）
  - 数据在拖拽过程中实时更新
  - 显示格式：「3.5 km · 约 12 分钟」
- [ ] **11. 分类色条强化**
  - 旅行块卡片左侧加一条 4px 宽的竖形色条，颜色对应分类
  - 颜色映射：景点=绿、美食=橙、住宿=紫、交通=蓝、购物=粉、活动=金
  - 灵感库中的旅行块也加同样色条，保持一致性
- [ ] **12. 操作反馈 toast**
  - 关键操作后显示短暂 toast 提示（2 秒自动消失）
  - 场景：保存成功、删除成功、投票已提交、坐标已更新
  - 样式：底部居中，深色半透明背景 + 白色文字 + 小图标
  - 动画：从底部滑入，150ms ease-out

### 📱 移动端体验
- [ ] **13. 长按弹出操作菜单**
  - 移动端长按旅行块卡片 500ms，弹出底部操作菜单
  - 菜单项：编辑详情、选择位置、备选分叉、添加评论、删除
  - 菜单从底部滑入，带 backdrop 遮罩
- [ ] **14. 底部快捷添加按钮**
  - 移动端画布底部固定一个圆形 ＋ 按钮
  - 点击弹出快速选择：新建行程 / 从灵感库选
  - 按钮带轻微阴影，滚动时不消失（fixed 定位）

### 🎨 CSS 代码质量
- [ ] **15. 提取 projects.html 内联样式**
  - 将 projects.html 中的 `<style>` 提取到 `projects.css`
  - 统一样式管理，与 styles.css 共享 CSS 变量
- [ ] **16. 提取 escapeHtml 工具函数**
  - 创建 `src/utils.js`，导出 `escapeHtml()`
  - app.js、map-view.js、projects.html 统一引用
- [ ] **17. 画布缩放改用 transform**
  - 将 `style.zoom` 改为 `transform: scale()` + `transform-origin: 0 0`
  - 解决 zoom 非标准属性的兼容性问题
- [ ] **18. CSS 长行拆分**
  - 将 styles.css 中超过 120 字符的单行规则拆为多行
  - 提升可读性和 diff 友好度

### 性能优化
- [ ] **虚拟滚动** - 大量行程项时的性能优化
- [ ] **图片懒加载** - 延迟加载图片
- [ ] **API 缓存** - 减少重复请求
- [ ] **离线支持** - Service Worker 缓存

### 数据安全
- [ ] **数据备份** - 自动备份项目数据
- [ ] **数据恢复** - 从备份恢复
- [ ] **操作日志** - 记录所有操作历史
- [ ] **数据导出** - 导出所有数据为 JSON

### 第三方集成
- [ ] **日历同步** - 同步到 Google Calendar
- [ ] **天气查询** - 显示目的地天气
- [ ] **汇率转换** - 国际旅行汇率转换
- [ ] **翻译功能** - 多语言支持

## 第七阶段：AI 增强 🟢 低优先级

- [ ] **真实 AI 接入** - 调用 LLM API 解析攻略
- [ ] **智能推荐** - 根据目的地推荐景点
- [ ] **自动排程** - AI 自动安排行程顺序
- [ ] **行程优化** - AI 优化行程路线
- [ ] **智能问答** - 回答关于行程的问题

---

## 当前进度

- ✅ 第一阶段：6/6 完成
- ✅ 第二阶段：6/6 完成
- ✅ 第三阶段：8/8 完成
- 🔴 第四阶段：16/21 已验收；实时协作 5 项待 Nginx 代理与双端验证
- 🟡 第五阶段：2/19 完成（通知中心与通知设置已验收）
- 🟢 第六阶段：0/18 完成（体验优化细化）
- 🟢 第七阶段：0/5 完成

**已验收：41/87；实时协作 5 项已实现待生产验收；地图 3 项已完成**

## 下一步重点

1. **优先体验优化**：点击空白新增 (#1)、弹窗缓动 (#5)、双击编辑 (#2)
2. **优先验收**：为 `/socket.io/` 配置反向代理，双浏览器测试实时协作
3. **随后完成**：键盘快捷键 (#6)、拖拽改时间 (#4)
4. **后续迭代**：地图集成、预算管理
